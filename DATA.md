# Data structure

In localStorage, an array of objects is stored under the key `fridge`, having the following structure:

```
[
    {
        product: String,
        expiration: Date,
        quantity: String
    },
    ...
]
```
